# Current handoff · 24 September 2026

The maintained public host is **https://aaddymehndi94.github.io/india-water-watch/**. The last verified live commit is recorded in `state/DEPLOYMENT_REPORT.md`. The G11 revision is being locally reviewed and will appear there only after its scoped push, GitHub Actions success and live smoke test. The previous secondary Sites deployment is not maintained. No unattended water-data update process exists.

**G11 source snapshot:** 22 source-linked claims, seven dated numeric observations, 16 evidence records, 43 geography records. IMD cumulative rainfall still ends 23 September; CWC monitored storage still dates to 10 September. New 24 September IMD heavy-rain counter-signal and 17 September issued forecast (valid 24–30 September) have distinct clocks. Three original Karnataka gazettes list 177 distinct drought-affected taluks; aid delivery and crop losses are not verified. A labeled, low-confidence AI Author assessment is conditional and auditable. `state/COVERAGE_AND_FRESHNESS.md` lists all remaining gaps.

**Current revision:** G11 story, dark/light design and three-stage atlas are integrated. Root and GitHub-path builds, source and browser review, exports and ZIPs are being finalized. Independent source review is `reporting/g11_independent_source_review.md`; independent visual/accessibility/security results will be `reporting/g11_quality_review.md`. The original hero is visibly labeled an AI-generated symbolic illustration. No human field/editorial review is claimed.

**Exact next executable action:** from this repository, run `git status --short`; ensure `reporting/g11_quality_review.md` is final; bind `state/RELEASE_REVIEW.json` to the current `dist/release.json` snapshot with hashes of `reporting/g11_data_review.md`, `reporting/g11_independent_source_review.md` and `reporting/g11_quality_review.md`; run `python tools/release_gate.py`; commit G11 with scoped `git add`; `git push origin main`; inspect the GitHub Pages Actions run and smoke-test the live homepage, theme, atlas, search and source/download links. Then record the deployed commit in `state/DEPLOYMENT_REPORT.md`.

Local root preview: `python -m http.server 8000 --directory dist-compact` and open `http://127.0.0.1:8000/`. Repository-path build: `PUBLIC_SITE_URL=https://aaddymehndi94.github.io PUBLIC_BASE_PATH=/india-water-watch/ npm exec --yes --package=pnpm@12.6.0 -- pnpm build`. Prompt-driven updates are in `docs/UPDATE_PLAYBOOK.md`. `pipeline/refresh.py` writes unapproved candidate material only.
