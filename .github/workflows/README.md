# No active workflows in this starter
The workflow specifications are under `templates/workflows/`. Codex creates and tests actual YAML only after the application commands and current immutable action versions are known. No public deployment, paid service, schedule or outside contact is authorized merely by committing this starter.
