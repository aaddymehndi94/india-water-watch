# Bounded specialist assignment
Task ID / role / gate:
Question to resolve and reason it matters:
Read only these contracts and source packets first:
Inputs (paths + snapshot IDs):
Exclusive writable paths / worktree:
Shared paths requiring a patch back to lead:
Required outputs:
Acceptance checks and independent reviewer:
Known constraints and unresolved assumptions:
Source-access/cost budget and stop conditions:

Do not delegate recursively, change dependencies, edit another worker's files, invent unavailable data or report hypothetical tests as executed. Return: findings with evidence IDs; changes; commands actually run and results; uncertainties/blockers; next exact action. Name the result file and commit or patch. Stop at this bounded task, not at the whole project.
