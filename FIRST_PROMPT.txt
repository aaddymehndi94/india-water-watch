You are the interactive editor-engineer lead for this repository. Build the India Water Watch project specified here, using native Codex subagents for bounded parallel work.

Read AGENTS.md, START_HERE.md, docs/00-TRUTH_RESET.md, docs/01-PROJECT_CHARTER.md, docs/08-AGENT_RUNBOOK.md, docs/09-EXECUTION_GATES.md and state/PROJECT_STATE.json. Then inspect the environment and the repository. Do not ask me to repeat requirements or choose routine implementation details.

This is not a request for another plan. Carry out the plan through a working, polished, locally tested, publication-ready candidate website and portable static deliverables. Begin with an independent audit of the prior claims. The existence, geography and severity of a 2026 crisis are questions to investigate, not conclusions to preserve.

Use the provided custom-agent roles when supported by the installed CLI. Verify the local Codex version and current official configuration reference first; do not silently overwrite my global settings or assume an outdated multi-agent syntax. Keep this lead session interactive. Use up to six workers concurrently, no recursive delegation, disjoint path ownership/worktrees, and independent review. If subagents are unavailable, record the limitation and execute the same role sequence without pretending agents ran.

Default architecture: Astro static output with React islands, TypeScript, Tailwind, shared D3/Observable Plot charts, versioned maps and static search. Resolve current compatible stable dependencies and freeze them. Do not create a server or paid dependency where a static publication and scheduled/offline update process will work.

Research all the editorial beats in docs/02-EDITORIAL_PLAN.md. Build the full national information architecture, real state/UT exploration, district coverage where evidence supports it, basin context, a useful historical comparator, source-linked journalism, official outlooks, conditional scenarios, recovery signals, public-information guides and a journalist desk. Make each visible chart and assertion auditable. A beautiful prototype with invented numbers is a failure.

Proceed autonomously with reversible local work. If a feed is inaccessible, use a legitimate alternate source/manual-import adapter, log the gap and continue. Do not fill missing data. Preserve existing user work. Do not spend money, create remote resources, publish, contact people, bypass access controls or accept licenses on my behalf without authorization.

After each meaningful gate, persist state and a scoped Git checkpoint. When interrupted, leave an exact next command. Build a live LOCAL progress page from actual task/test status, not invented percentages. Do not spend most of the project on this internal page.

Finish with the actual site preview, all feasible verified reporting, test evidence, screenshots at mobile/tablet/desktop, a host-ready dist directory and ZIP, journalist exports, coverage/freshness reports, a release report and easy prompt-driven update instructions. Run independent source/methodology and visual/accessibility/security reviews. Fix material failures before calling it ready. First public deployment remains a separate authorized step.

Start now with environment preflight, the evidence reset and the first parallel assignments. Continue until the local candidate and handoff are complete or a genuine external blocker prevents all remaining useful work.
